# Third-party dependencies actually imported by this repository.
# No versions are pinned; nothing here relies on a specific release.
numpy
scipy
matplotlib
pandas
astropy
pytest

# Used by the bundled helpers/ and totani_helpers/ packages (plotting style,
# progress bars, and MCMC/FITS I/O utilities).
Pillow
tqdm
cycler
